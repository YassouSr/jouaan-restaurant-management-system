import React from "react";

const HowItWorks = () => {
  return (
    <React.Fragment>
      <div>HowItWorks</div>
      <ul>
        <li>
          write the flow of website and steps from ordering to receiving the
          order
        </li>
        <li>
          write how to use mapbox map and if there isn't support for it write
          how to copy paste google map url
        </li>
        <li>write also how to use the website for staff (chef and drivers)</li>
      </ul>
    </React.Fragment>
  );
};

export default HowItWorks;
